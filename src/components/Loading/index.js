import LoadingIcon from 'images/loading.gif';

export default function Loading() {
    return (
        <div>
            <img src={ LoadingIcon } alt='Loading icon provided by Loading.io' width={50}/>
        </div>
    );
}